const mongo = {
  mongoURI:
    "YOUR_MONGO_URL_HERE",
  mongoUrlLocal: "YOUR_LOCAL_URL_HERE",
};
export default mongo;


// Add the mongo URLs in the en file so that it can imported as per the environment you select
